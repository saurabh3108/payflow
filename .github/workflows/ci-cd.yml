# ============================================
# PayFlow CI/CD Pipeline
# ============================================
# Triggers:
#   - Push to main/develop branch → Build, Push to GHCR
#   - Pull Request to main → Build only (validation)
# ============================================
# Features:
#   - Selective builds: Only builds services with changes
#   - Environment switching: dev (local) vs prod (AWS) - AWS deploy disabled for now
# ============================================

name: PayFlow CI/CD

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      force_build_all:
        description: 'Force build all services'
        required: false
        default: false
        type: boolean

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/payflow

jobs:
  # ============================================
  # STAGE 0: DETECT CHANGES
  # ============================================
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      api-gateway: ${{ steps.filter.outputs.api-gateway }}
      account-service: ${{ steps.filter.outputs.account-service }}
      transaction-service: ${{ steps.filter.outputs.transaction-service }}
      notification-service: ${{ steps.filter.outputs.notification-service }}
      frontend: ${{ steps.filter.outputs.frontend }}
      k8s: ${{ steps.filter.outputs.k8s }}
      any_service: ${{ steps.check.outputs.any_service }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Detect file changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            api-gateway:
              - 'services/api-gateway/**'
              - 'pom.xml'
            account-service:
              - 'services/account-service/**'
              - 'pom.xml'
            transaction-service:
              - 'services/transaction-service/**'
              - 'pom.xml'
            notification-service:
              - 'services/notification-service/**'
              - 'pom.xml'
            frontend:
              - 'frontend/**'
            k8s:
              - 'k8s/**'

      - name: Check if any service changed
        id: check
        run: |
          if [ "${{ steps.filter.outputs.api-gateway }}" == "true" ] || \
             [ "${{ steps.filter.outputs.account-service }}" == "true" ] || \
             [ "${{ steps.filter.outputs.transaction-service }}" == "true" ] || \
             [ "${{ steps.filter.outputs.notification-service }}" == "true" ] || \
             [ "${{ steps.filter.outputs.frontend }}" == "true" ] || \
             [ "${{ github.event.inputs.force_build_all }}" == "true" ]; then
            echo "any_service=true" >> $GITHUB_OUTPUT
          else
            echo "any_service=false" >> $GITHUB_OUTPUT
          fi

      - name: Summary of changes
        run: |
          echo "## 🔍 Change Detection Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Service | Changed |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|---------|" >> $GITHUB_STEP_SUMMARY
          echo "| API Gateway | ${{ steps.filter.outputs.api-gateway == 'true' && '✅ Yes' || '⏭️ No' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Account Service | ${{ steps.filter.outputs.account-service == 'true' && '✅ Yes' || '⏭️ No' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Transaction Service | ${{ steps.filter.outputs.transaction-service == 'true' && '✅ Yes' || '⏭️ No' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Notification Service | ${{ steps.filter.outputs.notification-service == 'true' && '✅ Yes' || '⏭️ No' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend | ${{ steps.filter.outputs.frontend == 'true' && '✅ Yes' || '⏭️ No' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| K8s Manifests | ${{ steps.filter.outputs.k8s == 'true' && '✅ Yes' || '⏭️ No' }} |" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # STAGE 1: BUILD & TEST (Selective)
  # ============================================
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.any_service == 'true' || github.event.inputs.force_build_all == 'true'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        if: needs.changes.outputs.api-gateway == 'true' || needs.changes.outputs.account-service == 'true' || needs.changes.outputs.transaction-service == 'true' || needs.changes.outputs.notification-service == 'true' || github.event.inputs.force_build_all == 'true'
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build API Gateway
        if: needs.changes.outputs.api-gateway == 'true' || github.event.inputs.force_build_all == 'true'
        run: |
          echo "🔨 Building API Gateway..."
          mvn clean package -DskipTests -pl services/api-gateway -am

      - name: Build Account Service
        if: needs.changes.outputs.account-service == 'true' || github.event.inputs.force_build_all == 'true'
        run: |
          echo "🔨 Building Account Service..."
          mvn clean package -DskipTests -pl services/account-service -am

      - name: Build Transaction Service
        if: needs.changes.outputs.transaction-service == 'true' || github.event.inputs.force_build_all == 'true'
        run: |
          echo "🔨 Building Transaction Service..."
          mvn clean package -DskipTests -pl services/transaction-service -am

      - name: Build Notification Service
        if: needs.changes.outputs.notification-service == 'true' || github.event.inputs.force_build_all == 'true'
        run: |
          echo "🔨 Building Notification Service..."
          mvn clean package -DskipTests -pl services/notification-service -am

      - name: Set up Node.js for Frontend
        if: needs.changes.outputs.frontend == 'true' || github.event.inputs.force_build_all == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Build Frontend
        if: needs.changes.outputs.frontend == 'true' || github.event.inputs.force_build_all == 'true'
        run: |
          echo "🔨 Building Frontend..."
          cd frontend
          npm ci
          npm run build

      - name: Build Summary
        run: |
          echo "## 🔨 Build Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Service | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| API Gateway | ${{ (needs.changes.outputs.api-gateway == 'true' || github.event.inputs.force_build_all == 'true') && '✅ Built' || '⏭️ Skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Account Service | ${{ (needs.changes.outputs.account-service == 'true' || github.event.inputs.force_build_all == 'true') && '✅ Built' || '⏭️ Skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Transaction Service | ${{ (needs.changes.outputs.transaction-service == 'true' || github.event.inputs.force_build_all == 'true') && '✅ Built' || '⏭️ Skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Notification Service | ${{ (needs.changes.outputs.notification-service == 'true' || github.event.inputs.force_build_all == 'true') && '✅ Built' || '⏭️ Skipped' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend | ${{ (needs.changes.outputs.frontend == 'true' || github.event.inputs.force_build_all == 'true') && '✅ Built' || '⏭️ Skipped' }} |" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # STAGE 2: PUSH TO GHCR (Selective)
  # ============================================
  push:
    name: Push to GHCR
    runs-on: ubuntu-latest
    needs: [changes, build]
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && needs.changes.outputs.any_service == 'true'
    
    permissions:
      contents: read
      packages: write

    outputs:
      image_tag: ${{ steps.meta.outputs.TAG }}
      sha_short: ${{ steps.meta.outputs.SHA_SHORT }}
      pushed_services: ${{ steps.pushed.outputs.services }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build all Java services
        run: mvn clean package -DskipTests

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        run: |
          if [ "${{ github.ref }}" == "refs/heads/main" ]; then
            echo "TAG=latest" >> $GITHUB_OUTPUT
          elif [ "${{ github.ref }}" == "refs/heads/develop" ]; then
            echo "TAG=develop" >> $GITHUB_OUTPUT
          else
            echo "TAG=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi
          echo "SHA_SHORT=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Build and Push API Gateway
        if: needs.changes.outputs.api-gateway == 'true' || github.event.inputs.force_build_all == 'true'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/api-gateway/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}-api-gateway:${{ steps.meta.outputs.TAG }}
            ${{ env.IMAGE_PREFIX }}-api-gateway:${{ steps.meta.outputs.SHA_SHORT }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and Push Account Service
        if: needs.changes.outputs.account-service == 'true' || github.event.inputs.force_build_all == 'true'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/account-service/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}-account-service:${{ steps.meta.outputs.TAG }}
            ${{ env.IMAGE_PREFIX }}-account-service:${{ steps.meta.outputs.SHA_SHORT }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and Push Transaction Service
        if: needs.changes.outputs.transaction-service == 'true' || github.event.inputs.force_build_all == 'true'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/transaction-service/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}-transaction-service:${{ steps.meta.outputs.TAG }}
            ${{ env.IMAGE_PREFIX }}-transaction-service:${{ steps.meta.outputs.SHA_SHORT }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and Push Notification Service
        if: needs.changes.outputs.notification-service == 'true' || github.event.inputs.force_build_all == 'true'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: services/notification-service/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}-notification-service:${{ steps.meta.outputs.TAG }}
            ${{ env.IMAGE_PREFIX }}-notification-service:${{ steps.meta.outputs.SHA_SHORT }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and Push Frontend
        if: needs.changes.outputs.frontend == 'true' || github.event.inputs.force_build_all == 'true'
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: frontend/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_PREFIX }}-frontend:${{ steps.meta.outputs.TAG }}
            ${{ env.IMAGE_PREFIX }}-frontend:${{ steps.meta.outputs.SHA_SHORT }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Track pushed services
        id: pushed
        run: |
          SERVICES=""
          if [ "${{ needs.changes.outputs.api-gateway }}" == "true" ] || [ "${{ github.event.inputs.force_build_all }}" == "true" ]; then
            SERVICES="${SERVICES}api-gateway,"
          fi
          if [ "${{ needs.changes.outputs.account-service }}" == "true" ] || [ "${{ github.event.inputs.force_build_all }}" == "true" ]; then
            SERVICES="${SERVICES}account-service,"
          fi
          if [ "${{ needs.changes.outputs.transaction-service }}" == "true" ] || [ "${{ github.event.inputs.force_build_all }}" == "true" ]; then
            SERVICES="${SERVICES}transaction-service,"
          fi
          if [ "${{ needs.changes.outputs.notification-service }}" == "true" ] || [ "${{ github.event.inputs.force_build_all }}" == "true" ]; then
            SERVICES="${SERVICES}notification-service,"
          fi
          if [ "${{ needs.changes.outputs.frontend }}" == "true" ] || [ "${{ github.event.inputs.force_build_all }}" == "true" ]; then
            SERVICES="${SERVICES}frontend,"
          fi
          echo "services=${SERVICES%,}" >> $GITHUB_OUTPUT

      - name: Push Summary
        run: |
          echo "## 🐳 Images Pushed to GHCR" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Tag:** \`${{ steps.meta.outputs.TAG }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Service | Status | Image |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| API Gateway | ${{ (needs.changes.outputs.api-gateway == 'true' || github.event.inputs.force_build_all == 'true') && '✅ Pushed' || '⏭️ Skipped' }} | ${{ (needs.changes.outputs.api-gateway == 'true' || github.event.inputs.force_build_all == 'true') && format('{0}-api-gateway:{1}', env.IMAGE_PREFIX, steps.meta.outputs.TAG) || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Account Service | ${{ (needs.changes.outputs.account-service == 'true' || github.event.inputs.force_build_all == 'true') && '✅ Pushed' || '⏭️ Skipped' }} | ${{ (needs.changes.outputs.account-service == 'true' || github.event.inputs.force_build_all == 'true') && format('{0}-account-service:{1}', env.IMAGE_PREFIX, steps.meta.outputs.TAG) || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Transaction Service | ${{ (needs.changes.outputs.transaction-service == 'true' || github.event.inputs.force_build_all == 'true') && '✅ Pushed' || '⏭️ Skipped' }} | ${{ (needs.changes.outputs.transaction-service == 'true' || github.event.inputs.force_build_all == 'true') && format('{0}-transaction-service:{1}', env.IMAGE_PREFIX, steps.meta.outputs.TAG) || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Notification Service | ${{ (needs.changes.outputs.notification-service == 'true' || github.event.inputs.force_build_all == 'true') && '✅ Pushed' || '⏭️ Skipped' }} | ${{ (needs.changes.outputs.notification-service == 'true' || github.event.inputs.force_build_all == 'true') && format('{0}-notification-service:{1}', env.IMAGE_PREFIX, steps.meta.outputs.TAG) || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Frontend | ${{ (needs.changes.outputs.frontend == 'true' || github.event.inputs.force_build_all == 'true') && '✅ Pushed' || '⏭️ Skipped' }} | ${{ (needs.changes.outputs.frontend == 'true' || github.event.inputs.force_build_all == 'true') && format('{0}-frontend:{1}', env.IMAGE_PREFIX, steps.meta.outputs.TAG) || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY

  # ============================================
  # STAGE 3: DEPLOYMENT SUMMARY
  # ============================================
  # Generates deployment commands for local K8s
  # AWS deployment is disabled for now - will be enabled later
  # ============================================
  deploy-summary:
    name: Deployment Summary
    runs-on: ubuntu-latest
    needs: [changes, push]
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'

    steps:
      - name: Generate Deployment Commands
        run: |
          echo "## 🚀 Deployment Ready" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Images pushed to GHCR with tag:** \`${{ needs.push.outputs.image_tag }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Services Updated:" >> $GITHUB_STEP_SUMMARY
          echo "\`${{ needs.push.outputs.pushed_services }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔄 Quick Deploy (Copy & Run in PowerShell):" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`powershell" >> $GITHUB_STEP_SUMMARY
          echo "# Navigate to project and pull latest" >> $GITHUB_STEP_SUMMARY
          echo "cd payflow; git pull origin ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Restart updated services to pull new images" >> $GITHUB_STEP_SUMMARY
          SERVICES="${{ needs.push.outputs.pushed_services }}"
          if [ -n "$SERVICES" ]; then
            IFS=',' read -ra SERVICE_ARRAY <<< "$SERVICES"
            for service in "${SERVICE_ARRAY[@]}"; do
              echo "kubectl rollout restart deployment/$service -n payflow" >> $GITHUB_STEP_SUMMARY
            done
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Verify rollout" >> $GITHUB_STEP_SUMMARY
          echo "kubectl get pods -n payflow" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📜 Or use deployment script:" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`powershell" >> $GITHUB_STEP_SUMMARY
          echo "./deploy-k8s.ps1 -RolloutOnly" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "> **Note:** AWS production deployment is disabled. Will be enabled after EC2 setup." >> $GITHUB_STEP_SUMMARY
